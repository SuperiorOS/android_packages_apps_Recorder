package org.lineageos.recorder.utils;

import static org.lineageos.recorder.utils.Utils.PREF_RECORDING_NOTHING;

public  class GlobalSettings {
    public static String sRecordingStatus = PREF_RECORDING_NOTHING;
}